#include<gtest/gtest.h>
#include"DynamicArray.h"

TEST(PushTest, BasicAssertions)
{
    DynamicArray new_array;
    // DynamicArray new_array(10);

    //new_array.pushBack(4);

    //EXPECT_EQ(new_array.getSize(),2);
}